ttkbootstrap>=1.10.1
watchdog>=3.0.0
requests>=2.31.0
psutil>=5.9.0
flask>=3.0.0
tkinterdnd2>=0.3.0   # optional — drag-and-drop support
pywin32>=306         # optional — Windows only, improves drive detection
